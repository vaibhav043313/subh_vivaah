<% content_for :title, "Feedback — Subh Vivaah" %>
<% content_for :head do %>
  <%= stylesheet_link_tag "site_pages", "data-turbo-track": "reload" %>
<% end %>

<div class="site-page">
  <%= render "shared/app_header" %>

  <main class="site-page__main">
    <div class="container">
      <%= render "shared/flash_strip" %>
      <h1 class="site-page__title">Feedback &amp; report an issue</h1>
      <p class="site-page__lead">
        Tell us what is working, what is not, or report inappropriate behaviour. We review every submission.
      </p>

      <% if @feedback.errors.any? %>
        <div class="site-form__errors" style="max-width: 28rem; margin-bottom: 1rem;">
          <strong>Please fix the following:</strong>
          <ul style="margin: 0.5rem 0 0; padding-left: 1.25rem;">
            <% @feedback.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with model: @feedback, url: submit_feedback_path, method: :post, local: true, class: "site-form card", style: "padding: 1.5rem 1.65rem;" do |f| %>
        <% unless user_signed_in? %>
          <div class="site-form__field">
            <%= f.label :email, "Email (so we can reply)" %>
            <%= f.email_field :email, required: true, autocomplete: "email" %>
          </div>
        <% end %>
        <div class="site-form__field">
          <%= f.label :category, "Category" %>
          <%= f.select :category, [["General feedback", "general"], ["Bug / technical issue", "bug"], ["Safety / report member", "safety"], ["Billing / subscription", "billing"]] %>
        </div>
        <div class="site-form__field">
          <%= f.label :body, "Details" %>
          <%= f.text_area :body, required: true, placeholder: "What happened? What did you expect?" %>
        </div>
        <%= f.submit "Submit feedback", class: "btn btn--primary", style: "align-self: flex-start;" %>
      <% end %>
    </div>
  </main>

  <%= render "shared/site_footer" %>
</div>
