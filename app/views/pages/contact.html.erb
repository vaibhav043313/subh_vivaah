<% content_for :title, "Contact us — Subh Vivaah" %>

<div class="<%= site_page_shell_class %>">
  <%= render "shared/app_header" %>

  <main class="<%= site_page_main_class %>">
    <div class="container">
      <%= render "shared/flash_strip" %>
      <h1 class="<%= site_page_title_class %>">Contact us</h1>
      <p class="<%= site_page_lead_class %>">
        Share your question or feedback. We read every message and reply as soon as we can.
      </p>

      <% if @contact_message.errors.any? %>
        <div class="<%= site_form_errors_class %>">
          <strong>Please fix the following:</strong>
          <ul class="mt-2 list-disc pl-5">
            <% @contact_message.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with model: @contact_message, url: submit_contact_path, method: :post, local: true, class: site_form_card_class do |f| %>
        <div class="<%= site_form_field_class %>">
          <%= f.label :name, "Your name" %>
          <%= f.text_field :name, required: true, autocomplete: "name" %>
        </div>
        <div class="<%= site_form_field_class %>">
          <%= f.label :email, "Email" %>
          <%= f.email_field :email, required: true, autocomplete: "email" %>
        </div>
        <div class="<%= site_form_field_class %>">
          <%= f.label :subject, "Subject (optional)" %>
          <%= f.text_field :subject, autocomplete: "off" %>
        </div>
        <div class="<%= site_form_field_class %>">
          <%= f.label :body, "Message" %>
          <%= f.text_area :body, required: true %>
        </div>
        <%= f.submit "Send message", class: "btn btn--primary self-start" %>
      <% end %>
    </div>
  </main>

  <%= render "shared/site_footer" %>
</div>
